import 'package:flutter/material.dart';

class EditTodoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('edit todoossoso'),
    );
  }
}
